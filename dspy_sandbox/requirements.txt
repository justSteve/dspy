# DSPy Learning Sandbox Requirements
# Install with: pip install -r requirements.txt
# (run from dspy_sandbox directory)

# Core DSPy framework (will pull all required dependencies)
dspy-ai>=3.0.0

# LLM Providers (for flexible provider support)
anthropic>=0.25.0
openai>=1.0.0

# Note: Other dependencies (litellm, cloudpickle, etc.) are automatically
# installed as dependencies of dspy-ai. They're included here for clarity.

# Utilities for enhanced experience
rich>=13.5.0
jupyter>=1.0.0
ipython>=8.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
